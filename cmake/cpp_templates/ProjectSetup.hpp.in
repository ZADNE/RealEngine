/**
 *  @author    Automatically generated file by RealProject
 */
#pragma once
#include <RealEngine/resources/hot_reload/HotReloadInitInfo.hpp>

namespace re::setup {

constexpr HotReloadInitInfo k_hotReloadInitInfo{
    .cmakePath              = "$<$<CONFIG:Debug>:${cmake_command}>",
    .sourceDir              = "$<$<CONFIG:Debug>:${source_dir}>",
    .binaryDir              = "$<$<CONFIG:Debug>:${binary_dir}>",
    .shaderFileExtensions   = "$<$<CONFIG:Debug>:${shader_file_extensions}>",
    .targetName             = "$<$<CONFIG:Debug>:${target}>",
    .targetBaseDirRel       = "$<$<CONFIG:Debug>:${target_base_dir_rel}>"
};

} // namespace re::setup
